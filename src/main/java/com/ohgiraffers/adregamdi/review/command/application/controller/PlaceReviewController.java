package com.ohgiraffers.adregamdi.review.command.application.controller;

import org.springframework.stereotype.Controller;

@Controller
public class PlaceReviewController {
//    @GetMapping("placeReviews")
//    public String findById(@PathVariable("boardId") int reviewNo, Model model, HttpSession session){
//         boardDetailDTO = bs.findById(reviewNo);
//        List<CommentDetailDTO> commentList = cs.findAll(reviewNo);
//        // memberId 세션값 가져오기
//        Long memberId = (Long) session.getAttribute(LOGIN_ID);
//
//        model.addAttribute("board",boardDetailDTO);
//        model.addAttribute("commentList",commentList);
//
//        bs.findLike(boardId,memberId);
//
//        int like = bs.findLike(boardId,memberId);
//        model.addAttribute("like",like);
//
//        return "/board/findById";
//    }

//    private ReviewRepository reviewRepository;

//    @PostMapping("/like")
//    public @ResponseBody int ReviewLike(int reviewNo, Long userNo) {
//        reviewRepository.save(reviewNo, userNo);
//        return result;
    }

